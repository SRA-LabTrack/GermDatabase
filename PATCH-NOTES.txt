CaneSprout v2.13.3 -> v2.13.4

Fixes the website login layout shown in the screenshot:
- removes legacy auth-shell 60/40 grid collision
- makes the showcase use the full browser width
- removes the inherited full-height white auth-panel slab
- keeps the photo slideshow visible across the whole page
- keeps wide hero copy left + compact glass login card right
- fixes login brand word spacing
- preserves responsive stacking on tablet/mobile
- bumps web/offline cache to v2.13.4

Overlay this ZIP into the permanent CaneSprout project and replace files.
Then run:
  PUBLISH-ALL-v2.13.4.cmd

The publish script uses GitHub main -> Vercel automatic deployment and does
not call the currently unreliable Vercel CLI deploy command.
