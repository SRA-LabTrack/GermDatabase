CaneSprout v2.13.8 -> v2.13.9 Variety Identity + Parentage Fix

Changes:
- Normalize variety display spacing around hyphens before future saves/imports.
- Phil 996-0339 / Phil 996- 0339 / Phil 996 -0339 all resolve to one canonical identity.
- Search/list results collapse accidental canonical duplicates.
- Three known historical conflict identities remain intentionally preserved.
- View Profile Parentage now shows explicit roles:
    <male> male X <female> female
- Registry preview uses the same Parentage format.
- Adds local read-only identity audit:
    npm.cmd run audit:variety-identities-local
- Current live Appwrite duplicate audit remains:
    npm.cmd run audit:duplicates

Overlay onto the current v2.13.8 project and replace files.
