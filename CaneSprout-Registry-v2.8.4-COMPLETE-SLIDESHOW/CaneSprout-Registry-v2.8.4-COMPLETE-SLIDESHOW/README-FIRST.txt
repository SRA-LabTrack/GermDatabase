CaneSprout DROP-IN DEV LAUNCHER

YOUR PROJECT PATH:
D:\Germ\CaneSprout-Registry-v2.8.4-COMPLETE-SLIDESHOW\CaneSprout-Registry-v2.8.4-COMPLETE-SLIDESHOW

HOW TO INSTALL

1. Extract this ZIP.
2. Copy BOTH items below into your project folder:

   START-CANESPROUT.cmd
   scripts\start-dev.mjs

3. Your project folder should then contain:

   package.json
   START-CANESPROUT.cmd
   scripts\
       start-dev.mjs

4. Double-click:

   START-CANESPROUT.cmd

The launcher will automatically run npm.cmd install if Vite is missing.

Expected output:

CaneSprout is running.
Open: http://localhost:5174

Keep the terminal open while using the website.
